1. Clonar el repo
2. Clonar el .env.template y renombrarlo a .env
3. Ejecutar `npm install` o `yarn install` 0 `pnpm install`
4. Veriricar que el backend esté corriendo
5. Ejecutar el comando `npm run dev` o `yarn install` 0 `pnpm dev`
